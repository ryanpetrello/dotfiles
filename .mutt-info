Install/Configure Mutt + Extras on OSX
======================================

$ git clone https://github.com/ryanpetrello/dotfiles.git
$ mv .mutt/mutt.rb /usr/local/Library/Formula/mutt.rb  # includes a patched mutt-sidebar
$ brew install mutt --with-sidebar-patch  # downloaded: 1.5.23_1.mavericks
$ brew install offlineimap
$ brew install curl-ca-bundle
$ brew install notmuch
$ brew install msmtp
$ brew install urlview
$ brew install elinks
$ brew install pngpaste
$ mv .mutt/org.ryan.mail.ryan.plist ~/Library/LaunchAgents/
$ launchctl load ~/Library/LaunchAgents/org.ryan.mail.ryan.plist
$ mv .mutt/homebrew.mxcl.offline-imap.plist ~/Library/LaunchAgents/
$ launchctl unload ~/Library/LaunchAgents/homebrew.mxcl.offline-imap.plist
$ launchctl load ~/Library/LaunchAgents/homebrew.mxcl.offline-imap.plist

$ cd ~/.mutt/osx-mail-handler/ && python setup.py py2app
$ mv dist/mutt-system-handler.app /Applications

Open Mail.app and in the `General` settings tab, set the default mail reader to `mutt-system-handler`.
System Preferences -> Users & Groups -> Login Items -> Add `mutt-system-handler`
